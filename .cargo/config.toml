# fetch git dependencies (ducktape-sdk, ducktape, and this repository itself —
# guest-builder reads every module out of it at a revision) with the git CLI
# instead of libgit2: a `url.insteadOf` rewrite to ssh breaks cargo's built-in
# fetcher, while the real git binary picks up the usual ssh agent/keys.
[net]
git-fetch-with-cli = true

[package]
name = "identity"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
abi = { workspace = true }
guest = { workspace = true }
borsh = { workspace = true }

[features]
default = ["program"]
# the wasm exports; off when another program links identity for its types
program = []

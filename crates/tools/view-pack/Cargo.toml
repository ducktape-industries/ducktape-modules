[package]
name = "view-pack"
version.workspace = true
edition.workspace = true

[dependencies]
wasmparser = "0.251"
wasm-encoder = "0.251"

[dev-dependencies]
view-wire = { workspace = true, features = ["manifest"] }
tempfile.workspace = true

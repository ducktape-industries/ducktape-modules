# the refusal classes: the tokens a refusal carries and the rule for minting
# one, as plain `&str` constants with NO dependencies, so a crate that must
# stay a leaf (duck-address as plain data, a wire type under a wasm view) names
# the same words the host does.
[package]
name = "refusal-class"
edition.workspace = true
version.workspace = true

[dependencies]

[package]
name = "store"
version.workspace = true
edition.workspace = true

# The optional typed layer over `guest`: the `Map`/`Set`/`Item` descriptors
# with `KeyCodec`, and `Page`/`PageReply`. Their methods take `guest`'s
# contexts. A view links it (and `guest`) and calls none of them.
[dependencies]
abi = { workspace = true }
borsh = { workspace = true }
guest = { workspace = true }
serde = { workspace = true }

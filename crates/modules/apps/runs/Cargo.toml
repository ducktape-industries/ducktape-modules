[package]
name = "runs"
edition.workspace = true
version.workspace = true

# Model configuration and run orchestration over dispatch, context pinning,
# payload composition, and deterministic model-output validation. Programmable
# accounts execute model-requested actions through their bound agent program.
[features]
# the wasm-guest port (src/guest.rs): the dispatch shell over
# ducktape-module-sdk. non-default and wasm-only — enabled solely by the
# guest-builder's synthesized cdylib workspace, never by the native gate.
guest = ["dep:ducktape-module-sdk"]
# the wasm index mapper shell (src/index_guest.rs): wires the pure run
# journal core into the fluent31 engine. wasm-only — enabled solely by
# `guest-builder --index`.
index-guest = ["index_guest/guest"]

[dependencies]
run-envelope = { workspace = true }
node-work = { workspace = true }
# wasm-only, behind `guest`: the port harness. a standalone-workspace path
# crate, pulled only when the guest build compiles this crate for wasm32.
ducktape-module-sdk = { workspace = true, optional = true }
sdk = { workspace = true }
borsh = { workspace = true }
saga = { git = "https://github.com/ducktape-industries/ducktape", branch = "dev", package = "saga", default-features = false }
# the duckfs wire types (FilesQuery::Refs / FilesReply::Refs / RefsInfo) the v1
# composer queries for the committed head it pins as source_snapshot (W2), plus
# `paths::canonical`. linked through the `files` MODULE's crate root (which
# re-exports duckfs-core) rather than duckfs-core directly, per the module rule:
# a module links other modules' wire surface, not their pure-core internals.
# default-features = false drops files' native `Files`/duckfs-disk (std::fs) so
# the `runs` guest graph stays pure.
files = { git = "https://github.com/ducktape-industries/ducktape", branch = "dev", package = "files", default-features = false }
duck-address = { workspace = true }
base64 = { workspace = true }
sha2 = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
async-trait = { workspace = true }
# the index CONTRACT (op-row envelope + the pure StateRead/Writes kit) — not
# the engine: the default surface is pure types, wasm32-clean, and is all the
# native build sees. the engine shell rides the `index-guest` feature.
index_guest = { workspace = true }

[dev-dependencies]
wasm-host = { workspace = true }
files = { git = "https://github.com/ducktape-industries/ducktape", branch = "dev", package = "files", features = ["native"] }
tempfile = { workspace = true }
syn = { version = "2", features = ["full"] }
# the host-side qmdb store handle the chat test fixtures inject.
statesync = { workspace = true }
# the shared programmable `TestCtx` (dev-only) — serves the dispatch sibling read.
sdk-testkit = { workspace = true }
futures = "0.3"
host = { workspace = true }
commonware-runtime = { workspace = true }
# forge is a DEV dependency ONLY — the sink emits a local Serialize mirror of
# ForgeMsg::OpenPr (production), and a conformance test pins that mirror against
# forge::decode_msg. keeping forge dev-only keeps vendored libgit2/git2 out of
# runs' production build + clippy graph. The NATIVE module (it re-exports its
# wire), because the tests drive a real `forge::Forge`.
forge = { git = "https://github.com/ducktape-industries/ducktape", branch = "dev", package = "forge" }
# the other NATIVE modules a programmable-run test composes. The same names in
# [dependencies] are the WIRE crates, so these carry a `-module` suffix and the
# test module aliases them back (`use dispatch_module as dispatch;`).
attribution-module = { git = "https://github.com/ducktape-industries/ducktape", branch = "dev", package = "attribution" }
capability-module = { git = "https://github.com/ducktape-industries/ducktape", branch = "dev", package = "capability" }
dispatch-module = { git = "https://github.com/ducktape-industries/ducktape", branch = "dev", package = "dispatch" }
governance-module = { git = "https://github.com/ducktape-industries/ducktape", branch = "dev", package = "governance" }
identity-module = { git = "https://github.com/ducktape-industries/ducktape", branch = "dev", package = "identity" }
modules-module = { git = "https://github.com/ducktape-industries/ducktape", branch = "dev", package = "modules" }
valset-module = { git = "https://github.com/ducktape-industries/ducktape", branch = "dev", package = "valset" }

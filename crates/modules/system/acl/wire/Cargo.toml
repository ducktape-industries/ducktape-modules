# The acl module's wire surface: its messages, queries, replies and their
# codecs, no module — the kernel host, the daemon and a sibling module link
# THIS and not the module behind it. Same standing as every other `-wire`
# crate: no `native` feature, no kernel host, no disk.
[package]
name = "acl-wire"
edition.workspace = true
version.workspace = true

[dependencies]
sdk = { workspace = true }
borsh = { workspace = true }
serde = { workspace = true }
